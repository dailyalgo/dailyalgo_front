import type { Meta, StoryObj } from '@storybook/react';
import { WrongReasonTagList } from './WrongReasonTagList';

const meta: Meta<typeof WrongReasonTagList> = {
	component: WrongReasonTagList,
	argTypes: {
		
}
	};

export default meta;
type Story = StoryObj<typeof WrongReasonTagList>;

export const Default: Story = {
	args: {
		
	}
};