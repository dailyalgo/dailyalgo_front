import type { Meta, StoryObj } from '@storybook/react';
import { FindIdForm } from './FindIdForm';

const meta: Meta<typeof FindIdForm> = {
	component: FindIdForm,
	argTypes: {
		
}
	};

export default meta;
type Story = StoryObj<typeof FindIdForm>;

export const Default: Story = {
	args: {
		
	}
};